import { Header } from "~components/Header";

export const App = () => {
  return (
    <Header
      onLogin={() => null}
      onLogout={() => null}
      onCreateAccount={() => null}
    />
  );
};
